<div class="row">
   <div class="col-lg-12">
     <h2>Reviews</h2><br>
   </div>
</div>
   <%= link_to  'NEW Comment (+) ', new_user_comment_path(@user) %><br>
<br>
<ul>
<% @comments.each do |comment| %>
<li>
<a href="/comments/<%= comment.id %>">
  <%= "#{comment.author} - #{comment.body}" %>
</a>
<li>
<% end %>
</ul>
